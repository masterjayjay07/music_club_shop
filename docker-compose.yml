version: "3.9"

services:
  api:
    container_name: api_music_club_shop
    build: ./
    ports:
      - 3000:3000
    depends_on:
      - db
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/app/:ro

  db:
    container_name: postgres
    image: "postgres:14.2-alpine"
    ports:
      - 5431:5432
    environment:
      - POSTGRES_USER=gabriel
      - POSTGRES_PASSWORD=173393
      - POSTGRES_DB=music_club_shop_db